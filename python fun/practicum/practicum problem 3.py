def generate_password(length):
    newpw = []
    import random
    while length > 0:
        newpw.extend += random.random()
        length - 1
    return newpw
        
        



length = int(input("Enter desired length"))
print(generate_password(length))
